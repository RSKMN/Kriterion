FROM eclipse-temurin:17-jdk-alpine
WORKDIR /app
COPY . .
RUN ./mvnw -q -DskipTests package
CMD ["java", "-jar", "target/kriterion-0.0.1-SNAPSHOT.jar"]

# Placeholder Dockerfile for backend. Tune JVM args and build steps for prod.
