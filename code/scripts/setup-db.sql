-- DB initialization placeholder for MySQL
CREATE DATABASE IF NOT EXISTS kriterion CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- Create sample tables via migrations (preferred) — use Flyway/Liquibase in prod.
