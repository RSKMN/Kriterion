# Controllers

Place REST controllers here. Keep controllers thin — delegate business logic to `service` layer.

Suggested naming: `AuthController`, `TransactionController`, `CategoryController`, etc.
