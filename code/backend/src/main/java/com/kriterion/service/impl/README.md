# Service Implementations

Concrete service implementations will live here once the domain logic starts. Keep interfaces in the parent service packages.

# Service Implementations

Service implementation classes go here (e.g., `TransactionServiceImpl`). Keep interfaces in parent `service` package.
